public class Main {
    BookDetails bookDetails=new BookDetails();
    bookDetails.chapter(bookDetails);
    bookDetails.index(bookDetails);
    bookDetails.book(bookDetails);
    bookDetails.dimensions(bookDetails);
}
